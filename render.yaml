services:
  - type: web
    name: platform-api
    env: python
    pythonVersion: "3.11"
    plan: free
    buildCommand: python -m pip install .
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
